<div class="mytask">
  <h1><%= @task.title %></h1>
  <div class="mytaskdetails">
    <p><%= @task.details %></p>
  </div>
   <p><p>
  <% if @task.completed %><input type="checkbox" checked disabled>This task is completed
  <% else %><input type="checkbox" disabled>This task is not completed yet<% end %>
   <p><p>
   <%= link_to "Edit this Task", edit_path %>
  <%= link_to "Back to list", index_path %>
</div>
